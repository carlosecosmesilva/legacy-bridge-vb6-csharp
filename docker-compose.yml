version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: paschoal
      POSTGRES_PASSWORD: paschoal_pass
      POSTGRES_DB: paschoal_db
    ports:
      - "5432:5432"
  api:
    build: ./Api
    ports:
      - "5000:80"
    environment:
      - ConnectionStrings__DefaultConnection=Host=db;Port=5432;Username=paschoal;Password=paschoal_pass;Database=paschoal_db
    depends_on:
      - db
